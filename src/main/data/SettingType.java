package main.data;

public enum SettingType
{
	SHOW_FOG,
	SHOW_FACING,
	SHOW_AWARENESS,
	ZIP_CACHED_FILES,	//currently unimplemented; performance is good enough for now with a large buffer and no compression
	MAX_NAME_LENGTH;
}
